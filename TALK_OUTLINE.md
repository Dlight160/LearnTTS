# 30 分钟技术分享：从 Mel 频谱到 VITS——TTS 学习之路

> 面向同事，分享 stage 1~3 的核心知识。

---

## 一、开场（2 分钟）

**一句话引子**：语音合成过去 5 年从"机械感严重"进化到"以假乱真"，今天带你看清背后的三个核心技术关卡。

**全局路线图**：
```
语音信号 → Mel 频谱（阶段1：语音的数字表示）
Mel 频谱 → 波形  （阶段2：HiFi-GAN 神经声码器）
文本 → 语音      （阶段3：VITS 端到端 TTS）
```

---

## 二、阶段1：Mel 频谱——是什么、为什么、怎么做（6 分钟）

### 1️⃣ 是什么：音频文件的浮点数 = 声波的"快照"

**先放一段波形图，再放对应的 Mel 频谱图：**

- **波形**：横轴 = 时间，纵轴 = 振幅。文件里的每个浮点数，就是"这个时刻声波振到了什么位置"
  - 正数 → 振膜向外推，负数 → 向里拉
  - 每秒 16000 个点连续播放 → 振膜震动 → 发出声音
- **频谱**：同一个声音的"频率视角"——横轴 = 时间，纵轴 = 频率，颜色 = 能量

**为什么从波形变到频谱？**
- 波形里你能看出响不响，但看不出"这声音是男声还是女声，是/a/还是/i/"
- 频谱一看：能量集中在低频 → 男声；能量在高频有横杠 → 摩擦音/s/
- **核心洞察**：语音的本质是"不同频率的能量随时间变化"，频谱比波形更直接

---

### 2️⃣ 为什么：TTS 不直接学波形，要用 Mel 频谱

**为什么不用原始波形（1 秒 16000~22050 个采样点）？**
- 维度太高，模型学不动
- 每个采样点对相位敏感，但人耳对相位很不敏感——模型花大量容量学人耳听不出的东西

**为什么不用线性频谱？**
- 线性频谱 1024 维，但人耳不是线性感知的：
  - 低频 100Hz→200Hz 你能听出音高变了（差一个八度）
  - 高频 10kHz→10.1kHz 你完全没感觉
- Mel 尺度是"按人耳感觉重新标尺"——低频 bin 窄、高频 bin 宽
- **结果**：80 维就够了，丢掉的是人耳不敏感的信息

**Mel 频谱在 TTS 中的角色**：
- 信息密度高：80 维 × ~86 帧/秒，序列长度比波形（22050 采样点/秒）压缩约 256 倍
- 感知相关：丢掉的是人耳不敏感的信息，保留的是语音内容/音色/韵律
- 成为 TTS 的"通用中间表示"——几乎所有模型（Tacotron2、FastSpeech、VITS）都用它

---

### 3️⃣ 怎么做：从音频文件到 Mel 频谱的完整链路

**第一步：理解音频文件本身（2 分钟）**

- **采样率**：每秒采多少个点。16kHz = 每秒 16000 个浮点数
  - 采样率决定最高能表示的频率（Nyquist：采样率 / 2）
  - 语音一般 16kHz 够了（覆盖人声主要能量范围），音乐需要 44.1kHz
  - **关键权衡**：采样率越高 → 数据量越大 → 模型越难学
- **量化**：每个采样点用多少 bit。16bit = 65536 个离散级别
- **PCM 编码**：最朴素的方式——直接把采样点的幅度值按顺序存下来

**第二步：STFT——从波形到频谱（2 分钟）**

```
波形（一维） → 切帧 → 每帧加窗 → FFT → 频谱（二维）
```

- **为什么切帧**：语音信号不平稳——10ms 前是/a/，10ms 后是/i/，不能当整体做 FFT
  - 帧长：~25ms，假设帧内信号平稳
  - 帧移（hop_length）：~10ms，相邻帧重叠 ~60%
- **加窗**：切帧会截断信号产生高频伪影 → 用 Hann 窗让帧首尾平滑衰减到零
- **FFT（快速傅里叶变换）**：把一帧时域信号变换到频域——得到该帧每个频率的能量
- **关键权衡**：
  - `n_fft` 越大 → 频率分辨率越高，但时间分辨率越低（不确定性原理在信号处理中的体现）
  - `hop_length` 越小 → 时间分辨率越高，但数据量越大

**第三步：Mel 滤波器组——从线性频谱到 Mel 频谱（1 分钟）**

```
线性频谱（1024 维）→ [Mel 滤波器组] → Mel 频谱（80 维）
```

- Mel 滤波器组是 n_mels 个三角滤波器，在 Mel 尺度上等距排列
- 每个滤波器的输出 = 线性频谱在该频率区间的加权和
- 这本质上是一次**降维 + 感知校准**：1024 维 → 80 维

**第四步：Griffin-Lim——为什么光有 Mel 不够（1 分钟）**

- STFT 输出是复数（幅度 + 相位），Mel 只保留了幅度，丢掉了相位
- GL 算法从幅度迭代猜测相位 → 重建音频
- **听感对比**：原始 vs GL 重建（明显"纸杯声"）
- **核心结论**：Mel 频谱是高效的表示，但从它重建回波形质量有限 → 引出阶段2

---

### 给观众留的印象
"Mel 是 TTS 的通用中间表示，但重建质量不够好——就像一张被压缩过的 JPEG，能看清内容但细节丢了。"

---

## 三、阶段2：HiFi-GAN 神经声码器——从 Mel 到波形的"超分辨率"（8 分钟）

### 核心问题
阶段1 的 GL 重建像"蒙着布说话"——有没有办法学一个解码器，能高质量地从 Mel 还原出波形？

先看清问题的规模：`Mel(80, T) → 波形(1, T×256)`，这是一个 **256 倍上采样**。

---

### 1. Generator——先听效果，再拆每一级贡献了什么

**先放一段 HiFi-GAN 合成的"大家好"，然后问**：这声音里的不同成分，分别是哪一级网络生成的？

#### Mel 本身提供了什么？——"一张粗略的草稿"
- Mel 每帧 80 维，告诉你"每个时刻大概有哪些频率的能量"
- 但它**丢掉了相位**，也**压缩了高频细节**（Mel 尺度高频 bin 宽，精度低）
- 所以 Mel 只是**内容骨架**：哪个音素、大概多响、大致音高

#### up1 + MRF1（×8）：从骨架到"节奏和响度"
- 从 Mel 的帧级信息展开到音节级——哪个字长、哪个字短、整体响度起伏
- **去掉这级会怎样**：声音变成匀速平调，像机器人念经，没有轻重缓急
- **类比**：画一个人，先画出"头在哪儿、身子在哪儿、手脚多长"——动态范围有了

#### up2 + MRF2（×8）：从节奏到"旋律轮廓"
- 填充共振峰过渡（/a/→/i/ 时嘴巴形状变化的过程）、基频 F0 的连续变化
- **去掉这级会怎样**：声音"平"，没有抑扬顿挫，像在念清单
- **类比**：画出五官的位置——眼睛在哪儿、嘴巴在哪儿——表情出来了

#### up3 + MRF3（×2）→ up4 + MRF4（×2）：从轮廓到"皮肤纹理"
- 补充高频谐波、摩擦音（/s/、/h/）的噪声纹理、气息声
- **去掉这级会怎样**：声音"糊"——能听清说什么，但不像真人的嗓音，少了"质感"
- **类比**：画上皮肤细节、头发丝——这最后两步把"像个人"变成"就是真人"

#### conv_post（32→1 + tanh）：从多通道特征合成单条波形
- 32 维特征 → 融合成 1 维波形
- tanh 限幅到 [-1, 1]：因为音频文件的浮点数范围就是 [-1, 1]

---

#### 为什么分 4 步，不一步到位 ×256？

一步 ×256 → kernel 需要覆盖 256 个位置，参数爆炸，且棋盘格伪影极其严重。

分 ×8→×8→×2→×2 的好处：
- **每步只学小幅拉伸**：kernel=16（×8 时）或 kernel=4（×2 时），只看"附近"的信息
- **每步后 MRF 及时抛光**：反卷积每次都会产生棋盘格伪影，及时修掉再进入下一轮，防止逐级累积
- **渐进式的信息增加**：粗节奏 → 旋律 → 纹理，每级只做自己擅长的事

---

#### 附录：MRF 为什么能抛光？

MRF = Multi-Receptive Field Fusion，三路并发后求和：

```
输入 → ResBlock(k=3,  dil=[1,3,5]) → 极高频纹理（如摩擦音的噪声）
输入 → ResBlock(k=7,  dil=[1,3,5]) → 中高频过渡（共振峰迁移）
输入 → ResBlock(k=11, dil=[1,3,5]) → 低频周期结构（基频 F0）
三路求和 → 输出
```

反卷积的"插零再卷积"会产生棋盘格伪影（波形上规律的微小抖动），MRF 的三路用不同感受野同时检查波形，哪路发现"这里不平滑"就修哪路。三把不同粗细的锉刀同时抛光，不留死角。

### 2. 为什么需要 GAN 训练？
- 只用 Mel Loss 训练 → 模型输出"平均化"的模糊波形（回归到均值）
- GAN 让判别器看完整波形（12800 维），发现 Mel 看不到的失真
- **三种 Loss 的分工**：

| Loss | 权重 | 作用 | 比喻 |
|------|------|------|------|
| Mel Loss | ×45 | 内容保真度，约束频谱包络 | 地图（内容别错） |
| GAN Loss | ×1 | 逼真度，骗过判别器 | 演技（自然不假） |
| FM Loss | ×2 | 稠密梯度，稳定训练 | 辅导（细节纠正） |

### 3. MPD + MSD 双判别器架构

- **MPD（多周期判别器）**：按周期重排 1D→2D，检查周期结构是否自然 → 防止音高抖动
- **MSD（多尺度判别器）**：下采样后看不同尺度整体质感 → 防止包络异常
- **为什么缺一不可**：
  - 只用 MPD → 周期对了但每个周期内部波形粗糙（音高对但沙哑）
  - 只用 MSD → 整体平滑但周期边界模糊（糊，像蒙布）
  - 两个制衡 → 同时做到"周期性正确"和"局部波形干净"

### 演示建议
放 GL 重建 vs HiFi-GAN 重建的对比音频，让听众感受差距。

### 给观众留的印象
"Mel→波形不是简单的反变换，而是一个生成问题——需要对抗训练来逼真。"

---

## 四、阶段3：VITS——从文本到语音的"端到端革命"（12 分钟）

### 前置：Tacotron2 的两大硬伤

两阶段 pipeline（Tacotron2 + HiFi-GAN）能出声，但上线时两个问题致命：
1. **自回归误差累积** — 逐帧预测 Mel，一步错步步错，容易 babbling 或尾部崩溃
2. **Soft Attention 对齐不稳定** — 可能跳词、重复词、对齐崩溃，是部署最大的拦路虎

VITS 用 MAS + 非自回归架构一次性解决了这两个问题，同时 VAE 框架带来了额外收益（多样性采样 + 端到端联合训练）。

### 1. VITS 用 VAE 框架解决了什么？

**核心思想**：让模型自己学一个"更好的 Mel"，而不是用人工设计的 Mel。

**架构概览**：
```
文本 → TextEncoder → h_text（音素级）
                        ↓
                  MAS 找对齐（训练时：真实时长）──→ 随机时长预测器 SDP（学习时长分布）
                        ↓                                    ↓
              h_text 按时长展开到帧级（训练用 MAS 时长，推理用 SDP 采样时长）
                        ↓
                  μ_p, σ_p（先验均值/方差）
                        ↓
                 Normalizing Flow（先验增强）
                        ↓
                   先验分布 p(z|c)
                        ↓ KL Loss
  真实波形 → STFT → 线性频谱 → PosteriorEncoder → 后验分布 q(z|x)
                        ↓
                    采样 z（192 维 × T 帧）
                        ↓
                HiFi-GAN Decoder → 波形
```

- **Posterior Encoder**：训练时"作弊通道"——从真实音频的线性频谱提取 z，告诉 Decoder "好的波形长什么样"
- 推理时扔掉 Posterior Encoder，只靠 TextEncoder 也能生成
- **z 是 192 维向量**（模型超参数 `inter_channels`），每维没有固定物理含义——模型自由分配维度编码不同类型信息（频谱、音色、韵律等）
- **z 携带的信息 ≥ Mel**：Mel Loss 作用在 decoder 输出波形上（生成波形→算 Mel→和 GT 比较），间接迫使 z 编码完整的频谱信息；同时 z 还额外携带相位等 Mel 丢弃的信息，192 维也比 80 维有更大的信息容量

### 1.5 KL Loss——训练的核心推动力

**一句话**：让先验分布（文本→p(z|c)）学会模仿后验分布（真实音频→q(z|x)），这样推理时扔掉后验也能生成好波形。

**为什么需要 KL Loss？**
- 训练时有两条路径都能生成 z：
  - 后验路径（作弊）：`真实音频 → PosteriorEncoder → q(z|x)` — 质量好，但推理时不能用
  - 先验路径（正式）：`文本 → TextEncoder → p(z|c)` — 推理时可用，但刚开始不准
- KL Loss 让 p(z|c) 去"贴近" q(z|x)，把后验的知识蒸馏到先验里

**直觉理解**：
- q(z|x) 像"标准答案"——从真实音频算出的理想隐变量
- p(z|c) 像"学生的答案"——从文本猜出来的隐变量
- KL 散度衡量两个分布的差距，值越小 → 先验越接近后验 → 推理生成质量越高

**数学形式**（口播用，不需深讲）：
```
KL(q(z|x) || p(z|c)) = log q(z|x) - log p(z|c)
```
展开后等价于：
- **重构项**：Decoder 输出的 Mel Loss（让 z 编码频谱信息）
- **先验匹配项**：约束 z 的分布接近标准高斯（经过 Flow 变换后）

**为什么要 Flow**：
- 单纯的 KL(q∥p) 要求 p 是简单高斯，表达能力有限
- Flow 把先验空间"揉变形"，让简单高斯变换成匹配复杂语音后验的形状
- 对应图中 Normalizing Flow 的位置——它在 KL 计算之前对先验做了**可逆变换**

**训练中的角色**：
| 损失项 | 作用 | 可类比 |
|--------|------|--------|
| KL Loss | 让先验学会后验，推理时扔掉后验也能用 | 知识蒸馏 |
| Mel Loss | 约束 z 能重建出正确波形 | 内容监督 |
| 总 Loss = KL + Mel | 互相制衡，缺一不可 | 准确 ≠ 生成 |

> **对比 Tacotron2**：Tacotron2 训练时没有"后验路径"这个概念——它直接在预测的 Mel 上算 L1 loss，缺少一个显式的隐空间对齐机制。VITS 的 KL Loss 让先验和后验在隐空间层面做匹配，比纯输出层监督更本质。

### 2. MAS（Monotonic Alignment Search）— 无监督自动对齐

#### 问题：音素和帧之间有一道"鸿沟"

- **文本侧**：一句话 10 个音素，每个音素对应一个 h_text 向量
- **音频侧**：这句话可能有 200 帧 Mel（~2 秒 × 100 帧/秒）
- **核心问题**：哪个音素对应哪些帧？这个映射叫"对齐"（alignment）

Tacotron2 用 Soft Attention 动态算对齐权重，但 Attention 可能跳词、重复、崩溃——这是部署最大的坑。

#### MAS 怎么做：把对齐变成一个"最短路径"问题

**第一步：构造相似度矩阵**

```
        帧1  帧2  帧3  ...  帧200
音素1  [0.8  0.3  0.1  ...   0.0]
音素2  [0.1  0.7  0.9  ...   0.0]
...
音素10 [0.0  0.0  0.0  ...   0.8]
```

矩阵每个格子 = h_text[音素] 和 z_frame[帧] 的余弦相似度（实际是高斯似然的 log 值）。相似度越高 → 这个音素"应该"对应这个帧。

**第二步：动态规划找单调路径**

约束规则（就是发音的物理规律）：
- **单调性**：音素 1→2→3... 不能反向，不能跳过音素（每个音素至少对应 1 帧）
- **连续性**：音素 i 对应的帧必须是连续的（不能音素 1 对应帧 1、3、5，跳着来）
- **起点固定**：音素 1 从帧 1 开始；终点固定：最后一个音素到最后一帧

用 Viterbi 算法（DP）在这些约束下找一条**累积相似度最大**的路径：

```
DP[i, j] = max(DP[i-1, j-1], DP[i, j-1]) + similarity[i, j]
```

- `DP[i-1, j-1]`：从上一个音素接过来
- `DP[i, j-1]`：当前音素延续到这一帧
- 两条路，保证单调和连续

**第三步：从路径提取时长**

找到路径后，数每个音素覆盖了多少帧 → 就得到了**时长序列** d = [3, 5, 8, 4, ...]

#### 为什么 MAS 比 Attention 稳定？

| | Tacotron2 Attention | VITS MAS |
|---|---|---|
| 对齐方式 | 每步动态计算注意力权重（soft） | 全局搜索最优路径（hard） |
| 单调性 | 靠 Loss 约束，间接保证 | 算法内建，数学保证 |
| 稳定性 | 可能跳词/重复/崩溃 | 不可能跳词，不可能重复 |
| 外部依赖 | 不需要 | 不需要 |

**核心洞察**：Attention 是"每步看一眼文本，决定读哪个音素"——它需要同时学对齐和生成，两个目标耦合在一起。MAS 是"先一次性解出最优对齐，再基于对齐做生成"——对齐和生成本质上解耦了。

#### 训练时的角色

MAS 在每个训练 step 都会重新跑一次：
1. 用当前的 TextEncoder 和 PosteriorEncoder 跑一遍，得到 h_text 和 z
2. MAS 在这两个表示之间找最优对齐
3. 用这个对齐把 h_text 展开到帧级，继续后续的 Flow → Decoder → 算 Loss
4. Loss 反向传播时同时优化 TextEncoder，让 h_text 和 z 的相似度越来越高

→ MAS 的对齐质量随训练**自动提升**，不需要额外监督信号。

### 3. Normalizing Flow 做先验增强
- 先验 p(z|c) 是简单高斯 → 不够灵活（语音的先验分布很复杂）
- Flow 层（Affine Coupling Layer）把高斯分布"揉捏"成更接近真实后验的形状

**Affine Coupling Layer 的直觉**：
```
输入 z → 切半 → z₁ 直接通过
               → z₁ → NN → scale, shift
               → z₂' = z₂ × exp(scale) + shift
         拼接 z₁ + z₂' → 输出
```
- 关键优势：**可逆**——反向只需算术逆，NN 不需要可逆
- 4 层交替切分通道，表达能力逐层累积

### 4. 随机时长预测器（Stochastic Duration Predictor）

#### 为什么需要 SDP？——MAS 的"能看不能用"困境

- MAS 需要**真实音频的 z**（来自 PosteriorEncoder）才能算对齐
- 推理时没有真实音频 → 没有 z → MAS 不能跑
- 所以训练时用 MAS 得到"标准答案"时长 d，但推理时需要一个**不看音频就能预测时长**的模块 → SDP

#### 为什么是"随机"的，不是直接回归？

**一句话**：同一句话同一个人，每次念的语速不可能完全一样。直接回归时长（预测一个确定值 d）会丢失这种自然的多样性。

SDP 不预测"时长是多少"，而是预测"时长的概率分布"——"这个音素大概 3~5 帧，最可能是 4 帧"。

```
确定性预测：音素 /a/ → 4 帧（每次一样）
SDP 预测：  音素 /a/ → 𝒩(4, 0.5)（每次采样不同）
```

#### SDP 内部结构

SDP 是一个 **Flow-based 生成模型**，和 Normalizing Flow 同样的原理：

```
h_text（音素级）→ 条件编码 → Flow 层 × N → 时长分布 p(d | h_text)
                                                 ↓
                                   训练：最大化 log p(d_gt | h_text)（MAS 给的时长）
                                   推理：从 p(d | h_text) 采样 → 时长序列
```

**Flow 的作用**：
- 输入是简单高斯噪声，Flow 把它变换成复杂分布
- h_text 作为条件（condition）控制变换方向——不同音素得到不同分布
- **可逆**保证了可以算精确的 log-likelihood，训练稳定

#### 训练 vs 推理

| | 训练 | 推理 |
|---|---|---|
| 时长来源 | MAS（需要真实音频） | SDP 采样 |
| SDP 在干嘛 | 学习——最大化 MAS 时长的似然 | 干活——从学到的分布采样 |
| Loss | Negative Log-Likelihood（对 MAS 时长） | 无 |

**设计精妙之处**：SDP 的学习目标不是某个手工标注，而是 MAS 自动发现的时长——MAS 越好，SDP 学到的就越准。两个模块**互相配合但互不依赖**。

#### 多样性——SDP 带来的"意外之喜"

同一文本采样不同时长序列 → 语速变化：

```
文本： "今天天气真好"
采样1： d = [4, 3, 5, 6, 8, 5] → 正常语速 ~2.0s
采样2： d = [6, 5, 8, 10, 12, 8] → 偏慢语速 ~3.2s
采样3： d = [2, 2, 3, 4, 5, 3] → 偏快语速 ~1.2s
```

音素的相对比例也会随机变化——"天"可能比"气"长 50%，也可能只长 20%。这让合成听起来更自然，不像"机器人总是用完全一样的节奏说话"。

#### 和 Tacotron2 的对比

| | Tacotron2 | VITS |
|---|---|---|
| 时长预测 | Attention 隐式决定每帧读哪个音素（间接） | SDP 显式预测每个音素的帧数 |
| 多样性 | 无——自回归生成是确定性的 | 有——时长采样带来语速/节奏变化 |
| 可控性 | 难以控制语速 | 可手动缩放时长序列控制语速 |

### 5. VITS 的后续影响（承上启下）

**VITS 之后 TTS 分岔为两条路线**：
```
VITS (VAE + Flow + 端到端)
  ├── CosyVoice：Flow Matching + 连续表示（质量上限高，架构自由）
  └── FishSpeech：离散 token + LLM（享受 Scaling Law，可扩展）
```

**一句话总结三条路线的演化**：
- **Tacotron2 → VITS**：从 AR 到 NAR，从两阶段到端到端，用 MAS 解决对齐
- **VITS → CosyVoice**：从 Flow（架构受限）到 Flow Matching（可用 Transformer），从一步生成到多步容错
- **VITS → FishSpeech**：从连续隐变量到离散 token，接入 LLM 生态获得 Scaling Law

### 给观众留的印象
"VITS 用隐变量 z 替代 Mel，让模型自己学最优中间表示——这是 TTS 从'手工设计'走向'端到端学习'的分水岭。"

---

## 五、总结（3 分钟）

### 三阶段的技术接力

| 阶段 | 输入 → 输出 | 核心问题 | 解决方案 |
|------|------------|---------|---------|
| 阶段1：Mel 频谱 | 波形 → Mel 频谱 | 波形太"死板"，线性频谱维度太高 | Mel 尺度做感知压缩，80 维保留关键信息 |
| 阶段2：HiFi-GAN | Mel → 波形 | GL 重建像"纸杯声"，质量不够 | GAN 对抗训练 + MPD/MSD 双判别器 |
| 阶段3：VITS | 文本 → 波形 | AR 误差累积 + Attention 对齐不稳定 | MAS 稳定对齐 + NAR 并行生成 + VAE 端到端 |

**完整链路**：
```
"大家好" → 音素序列 /d/a/j/i/a/h/a/o/ → [TextEncoder] → z → [HiFi-GAN] → 波形 ✓
```

### 可以留的尾巴
> "VITS 之后，TTS 分岔为 CosyVoice（Flow Matching）和 FishSpeech（LLM 离散 token）两条路线。如果大家感兴趣，下次可以分享 LLM-based TTS 的原理。"

---

## 附录：演讲建议

### 演示材料准备
1. [ ] **对比音频**（2-3 段）：原始 → GL 重建 → HiFi-GAN 重建（stage2 的 compare.py 可生成）
2. [ ] **Mel 频谱图**（1 张，岩浆 colormap）
3. [ ] **VITS 合成 demo**（1 段，哪怕训练没完全收敛）
4. [ ] **架构串联图**：三个阶段的箭头图，标出"输入→输出"

### 时间分配
| 环节 | 时间 | 备注 |
|------|------|------|
| 开场 + 路线图 | 2 min | 全局框架，别超时 |
| 阶段1：语音基础 + Mel | 6 min | GL 重建放音频对比 |
| 阶段2：HiFi-GAN | 8 min | 重点讲 GAN 动机 + 双判别器 |
| 阶段3：VITS | 12 min | 核心部分，VAE + MAS + 三种路线对比 |
| 总结 | 2 min | 技术接力图 + 后续方向 |

### 如果时间紧张
- 砍阶段1 的 GL 算法细节（保留结论即可）
- 保阶段3 的 VAE 框架和路线对比——这是最有价值的深度理解