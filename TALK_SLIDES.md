# PPT 内容稿：从 Mel 频谱到 VITS——TTS 学习之路

> 与 `TTS_Talk_SignalFlow.pptx` 对应，共 23 页

---

## Slide 1 · 封面

**从 Mel 频谱到 VITS——TTS 学习之路**

30 分钟技术分享

---

## Slide 2 · 全局路线图

```
语音信号 → Mel 频谱　　　阶段1：语音的数字表示
Mel 频谱 → 波形　　　　　阶段2：HiFi-GAN 神经声码器
文本　　→ 语音　　　　　阶段3：VITS 端到端 TTS
```

---

## Slide 3 · 阶段1 概览：Mel 频谱

| # | 主题 | 要点 |
|---|------|------|
| 01 | 波形 vs 频谱 | 同一个声音的两种视角 |
| 02 | 为什么用 Mel | 三种表示的对比 —— 为什么是 80 维 |
| 03 | Mel 怎么做 | 切帧 → 加窗 → FFT → Mel 滤波器组 |
| 04 | Mel 的局限 | Griffin-Lim 重建 —— 引出阶段2 |

---

## Slide 4 · 波形 vs 频谱

**波形**：横轴=时间，纵轴=振幅 → 看不出"这声音是男声还是女声"

**频谱**：横轴=时间，纵轴=频率，颜色=能量 → 一眼看出音色、音高、内容

> 语音的本质 = 不同频率的能量随时间变化

---

## Slide 5 · 为什么用 Mel 频谱？

| 方案 | 问题 |
|------|------|
| 原始波形 | 1秒 16000+ 点，维度太高，对相位过敏感 |
| 线性频谱 | 1024 维，人耳非线性感知（低频敏感，高频迟钝） |
| **Mel 频谱** | 按人耳感知压缩 → **80 维** |

压缩比：80维 × 86帧/秒，比波形压缩约 **256 倍**

---

## Slide 6 · Mel 频谱怎么做？

```
波形 → 切帧(25ms) → 加窗(Hann) → FFT → Mel滤波器组 → Mel频谱(80维)
```

关键参数：n_fft（频率 vs 时间分辨率）、hop_length（帧移）、n_mels=80

---

## Slide 7 · Mel 的局限 → 引出阶段2

Griffin-Lim 从 Mel 重建波形 → **"纸杯声"**

> Mel 是 TTS 的通用中间表示，但重建质量不够 — 像被压缩过的 JPEG

🎤 放 GL 重建 vs 原始音频对比

---

## Slide 8 · 阶段2 概览：HiFi-GAN

| # | 主题 | 要点 |
|---|------|------|
| 01 | 核心问题 | Mel(80,T) → 波形(1,T×256)，256倍上采样 |
| 02 | Generator 逐级上采样 | ×8 → ×8 → ×2 → ×2，每级贡献不同 |
| 03 | MRF 三路抛光 | 不同感受野同时检查，不留死角 |
| 04 | 三种 Loss 的分工 | Mel Loss（内容）+ GAN Loss（逼真）+ FM Loss（稳定） |
| 05 | MPD + MSD 双判别器 | 周期正确 + 局部波形干净，缺一不可 |

---

## Slide 9 · HiFi-GAN 核心问题

```
Mel (80, T)  →  波形 (1, T×256)
```

**256 倍上采样** → 不是简单的反变换，是生成问题

用 GAN 对抗训练 + 逐级上采样 + MRF 抛光

---

## Slide 10 · Generator：渐进式上采样

| 阶段 | 倍率 | 贡献 |
|------|------|------|
| up1 + MRF1 | ×8 | 节奏和响度 — "头在哪儿、身子在哪儿" |
| up2 + MRF2 | ×8 | 旋律轮廓 — "五官位置，表情出来了" |
| up3 + MRF3 | ×2 | 高频谐波纹理 — "皮肤纹理" |
| up4 + MRF4 | ×2 | 气息/摩擦音细节 — "就是真人" |

分 4 步原因：避免棋盘格伪影，每步由 MRF 及时抛光

---

## Slide 11 · MRF：三路抛光

| 卷积核 | 感受野 | 负责 |
|--------|--------|------|
| k=3, dil=[1,3,5] | 极高频纹理 | 摩擦音噪声 |
| k=7, dil=[1,3,5] | 中高频过渡 | 共振峰迁移 |
| k=11, dil=[1,3,5] | 低频周期结构 | 基频 F0 |

三路求和 → 光滑波形

---

## Slide 12 · 三种 Loss 分工

| Loss | 权重 | 作用 | 比喻 |
|------|------|------|------|
| Mel Loss | ×45 | 内容保真度 | 地图 — 内容别错 |
| GAN Loss | ×1 | 逼真度 | 演技 — 自然不假 |
| FM Loss | ×2 | 稠密梯度，稳定训练 | 辅导 — 细节纠正 |

---

## Slide 13 · MPD + MSD 双判别器

| | MPD | MSD |
|------|------|------|
| 做法 | 按周期重排 1D→2D | 下采样看不同尺度 |
| 检查 | 周期结构是否自然 | 整体质感 |
| 防止 | 音高抖动 | 包络异常 |

缺一不可：周期正确 + 局部波形干净

---

## Slide 14 · 阶段3 概览：VITS

| # | 主题 | 要点 |
|---|------|------|
| 01 | Tacotron2 的两大硬伤 | 自回归误差累积 + Attention 对齐不稳定 |
| 02 | VITS 架构概览 | VAE 框架：用隐变量 z 替代 Mel |
| 03 | MAS 无监督对齐 | 动态规划找最优单调路径 |
| 04 | SDP 随机时长预测器 | 预测时长分布，采样带来语速变化 |
| 05 | Normalizing Flow | 把简单高斯「揉捏」成复杂分布 |
| 06 | KL Loss | 训练核心推动力 — 让先验学会后验 |
| 07 | VITS 的后续影响 | CosyVoice + FishSpeech |

---

## Slide 15 · Tacotron2 两大硬伤

| 问题 | 表现 |
|------|------|
| 自回归误差累积 | 逐帧预测 Mel，一步错步步错，尾部崩溃 |
| Soft Attention 不稳定 | 跳词、重复词、对齐崩溃 |

→ VITS 用 MAS + 非自回归一次性解决

---

## Slide 16 · VITS 架构概览

训练流程（左栏，有真实音频）：
```
文本 + 真实波形
  ↓          ↓
TextEncoder  STFT→线性频谱
  ↓          ↓
MAS找对齐   PosteriorEncoder
  ↓          ↓
按时长展开   q(z|x)
  ↓    ↕ KL Loss
Normalizing Flow → p(z|c) → 采样z → Decoder → 波形
```

推理流程（右栏，只有文本）：
```
文本 → TextEncoder → SDP预测时长 → 按时长展开
  → Normalizing Flow → p(z|c) → 采样z → Decoder → 波形
```

---

## Slide 17 · MAS 无监督对齐

问题：10 个音素 vs 200 帧 → 谁对应谁？

方案：动态规划找最优单调路径
- 构造相似度矩阵 → DP 求最大累积相似度路径
- 约束：单调、连续、每个音素至少 1 帧

| | Tacotron2 Attention | VITS MAS |
|---|---|---|
| 对齐方式 | 每步动态计算（soft） | 全局搜索最优（hard） |
| 单调性 | Loss 间接约束 | 算法内建，数学保证 |
| 稳定性 | 可能跳词/崩溃 | 不可能跳词/重复 |

---

## Slide 18 · SDP 随机时长预测器

为何需要：MAS 需要真实音频 → 推理时没有 → SDP 替代

为何随机：预测时长分布而非确定值 → 每次采样不同 → 语速自然

训练：最大化 MAS 时长的似然
推理：从学到的分布采样

---

## Slide 19 · Normalizing Flow

问题：先验 p(z|c) 是简单高斯 → 不够灵活

方案：Flow 把高斯「揉捏」成复杂分布

Affine Coupling Layer：切半 → NN 预测 scale/shift → 变换一半 → 拼接

关键优势：可逆 — 反向只需算术逆，NN 不需要可逆

---

## Slide 20 · KL Loss

| 路径 | 来源 | 用途 |
|------|------|------|
| 后验 q(z\|x) | 真实音频 → PosteriorEncoder | 训练"标准答案" |
| 先验 p(z\|c) | 文本 → TextEncoder | 推理时可用 |

KL Loss 让先验学会后验 → 越小 → 推理质量越高

对比 Tacotron2：缺少显式隐空间对齐机制

---

## Slide 21 · VITS 后续影响

```
VITS (VAE + Flow + 端到端)
  ├── CosyVoice：Flow Matching + 连续表示
  └── FishSpeech：离散 token + LLM
```

三条演化：AR→NAR · 两阶段→端到端 · Flow→Flow Matching/LLM

---

## Slide 22 · 三阶段技术接力

| 阶段 | 输入→输出 | 核心问题 | 解决方案 |
|------|----------|---------|---------|
| Mel 频谱 | 波形→Mel | 维度高 | Mel 感知压缩，80维 |
| HiFi-GAN | Mel→波形 | GL 重建质量差 | GAN + MPD/MSD |
| VITS | 文本→波形 | AR 误差 + 对齐 | MAS + NAR + VAE |

完整链路：「大家好」→ 音素 → TextEncoder → z → HiFi-GAN → 波形 ✓

---

## Slide 23 · Thank You

**从 Mel 频谱到 VITS——TTS 学习之路**

> 下次分享：LLM-based TTS 原理

Q & A

---

## 设计说明

- **风格**：极简亮色（Signal Flow 变体），米白底 + 暖奶油卡片 + 琥珀强调
- **字体**：Georgia 标题 / Calibri 正文
- **装饰**：每页顶部琥珀横线+圆点，底部波形竖条
- **架构图**：训练/推理左右双栏竖排版
